# Dependencies (Vercel installs these)
node_modules
frontend/node_modules

# Version control
.git

# Local env (use Vercel dashboard for production env vars)
.env
.env.local
.env.*.local

# Local dev data files (Vercel uses /tmp for serverless)
bookings.json
chef_registrations.json
contacts.json

# Logs and temp
*.log
.DS_Store
